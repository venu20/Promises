import { whenAllDone } from './customMethods';

describe('When All Done', () => {
  it('should return an array of resolved promises', () => {

  });
  it('should return an array of rejected promises', () => {

  });
})